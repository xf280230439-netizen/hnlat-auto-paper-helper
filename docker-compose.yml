version: '3.8'

services:
  hnlat-downloader:
    build: .
    container_name: hnlat-downloader
    ports:
      - "5000:5000"
    volumes:
      - ./config.json:/app/config.json
      - ./downloads:/app/downloads
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
